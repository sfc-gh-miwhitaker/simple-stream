# Examples

- `custom_simulation.py` – runs a short-duration simulation (2 minutes) at 50 events per second using the same configuration as the CLI. Use it as a starting point for bespoke workloads.

Run with:
```bash
python examples/custom_simulation.py
```
